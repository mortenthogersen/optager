transformers>=4.46.0
torch>=2.0.0
torchaudio>=2.0.0
librosa>=0.10.0
numpy>=1.24.0

# GPU (valgfri — vælg én afhængigt af GPU)
# NVIDIA:       torch fungerer out-of-the-box med CUDA
# AMD Windows:  pip install torch-directml
# AMD Linux:    brug torch med ROCm fra https://pytorch.org
# Apple:        torch fungerer out-of-the-box med MPS
